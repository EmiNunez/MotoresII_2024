using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NoLLegoElProfeEvent : GameEvent
{
  public NoLLegoElProfeEvent(params object[] _list) : base(_list) // Llama al constructor del padre para que guarde los datos.
    {

    }
}
